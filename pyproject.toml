[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "jupyter-distributed"
version = "0.1.0"
description = "A persistent multi-process SPMD kernel for JupyterLab"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
authors = [
  { name = "Jupyter Distributed contributors" },
]
keywords = ["jupyter", "jupyterlab", "pytorch", "distributed", "spmd"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Framework :: Jupyter",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
]
dependencies = [
  "ipykernel>=6.29",
  "jupyter-client>=8.6",
  "jupyter-server>=2.14",
  "jupyterlab>=4.2",
]

[project.optional-dependencies]
distributed = ["torch>=2.4"]

[project.scripts]
jupyter-distributed-install-kernelspec = "jupyter_distributed.kernelspec:main"

[dependency-groups]
dev = [
  "build>=1.2",
  "nbstripout>=0.9.1",
  "pytest>=8.2",
  "pytest-asyncio>=0.23",
  "ruff>=0.6",
]

[tool.hatch.build.targets.wheel]
packages = ["jupyter_distributed"]
artifacts = ["jupyter_distributed/labextension/**"]

[tool.hatch.build.targets.wheel.shared-data]
"jupyter-config/jupyter_server_config.d/jupyter_distributed.json" = "etc/jupyter/jupyter_server_config.d/jupyter_distributed.json"
"jupyter-config/kernels/jupyter-distributed/kernel.json" = "share/jupyter/kernels/jupyter-distributed/kernel.json"
"jupyter_distributed/labextension" = "share/jupyter/labextensions/jupyter-distributed"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
